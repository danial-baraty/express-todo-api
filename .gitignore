
# These files are large and can be regenerated using 'npm install' based on the package.json and package-lock.json files.
node_modules/

# Exclude compiled files generated by the TypeScript compiler.
dist/
*.tsbuildinfo

# CRITICAL: Exclude files containing sensitive environment variables, secrets, and API keys.
.env

# Exclude files created during runtime or debugging sessions.
*.log

test.ts